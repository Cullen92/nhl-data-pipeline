name: Deploy dbt Docs to GitHub Pages

on:
  push:
    branches: [ main ]
    paths:
      - 'dbt_nhl/**'
      - '.github/workflows/dbt-docs.yml'
  workflow_dispatch:  # Allow manual trigger

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dbt
        run: |
          python -m pip install --upgrade pip
          pip install dbt-snowflake==1.7.0

      - name: Install dbt packages
        working-directory: dbt_nhl
        run: dbt deps

      - name: Generate dbt docs (no database connection needed)
        working-directory: dbt_nhl
        env:
          # Dummy credentials - docs generate doesn't need real connection
          # when using --no-compile flag
          SNOWFLAKE_USER: "docs_generator"
          SNOWFLAKE_PASSWORD: "not_used"
        run: |
          # Generate docs without compiling (uses cached manifest if available)
          # For full docs, we'd need real credentials, but static docs work fine
          dbt docs generate --no-compile || dbt docs generate --empty-catalog

      - name: Prepare docs for GitHub Pages
        run: |
          mkdir -p _site
          cp dbt_nhl/target/index.html _site/
          cp dbt_nhl/target/catalog.json _site/ 2>/dev/null || echo '{}' > _site/catalog.json
          cp dbt_nhl/target/manifest.json _site/ 2>/dev/null || echo '{}' > _site/manifest.json

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
